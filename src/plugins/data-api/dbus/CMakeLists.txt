cmake_minimum_required(VERSION 2.8)

file(GLOB WNTRDATA_DBUS_CPP "*.cpp")
file(GLOB WNTRDATA_DBUS_HPP "*.hpp")

list(APPEND WNTRDATA_SCPP WNTRDATA_DBUS_CPP)

## Installs
install(FILES ${WNTRDATA_DBUS_HPP}
    DESTINATION "${WNTRDATA_INCLUDE_DIR}/dbus"
    COMPONENT dev)
