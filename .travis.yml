language: cpp
compiler:
  - gcc
  - clang

before_install:
  - test/bootstrap --before

before_script:
  - test/boostrap --generate

script:
  - test/bootstrap --run

after_success:
  - test/bootstrap --post
